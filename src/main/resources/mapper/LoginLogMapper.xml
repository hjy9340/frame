<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sgg.frame.modulers.system.mapper.LoginLogMapper">
  <resultMap id="BaseResultMap" type="com.sgg.frame.modulers.system.entity.LoginLog">
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="logname" jdbcType="VARCHAR" property="logname" />
    <result column="userid" jdbcType="INTEGER" property="userid" />
    <result column="createtime" jdbcType="TIMESTAMP" property="createtime" />
    <result column="succeed" jdbcType="VARCHAR" property="succeed" />
    <result column="ip" jdbcType="VARCHAR" property="ip" />
    <result column="message" jdbcType="LONGVARCHAR" property="message" />
  </resultMap>
  <select id="getLoginLog" resultType="map">
    select * from sys_login_log where 1=1
    <where>
      <if test="beginTime!=null and beginTime!='' and endTime!=null and endTime!=''">
        and createtime between concat(#{beginTime},' 00:00:00') and concat(#{endTime},' 23:59:59'))
      </if>
      <if test="logName!=null and logName!=''">
        and logname like concat('%',#{logname},'%')
      </if>
      <choose>
        <when test="orderByField != null and orderByField !=''">
          <choose>
            <when test="isAsc">
              order by ${orderByField} ASC
            </when>
            <otherwise>
              order by ${orderByField} DESC
            </otherwise>
          </choose>
        </when>
        <otherwise>
          order by createtime DESC
        </otherwise>
      </choose>
    </where>
  </select>
</mapper>